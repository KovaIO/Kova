{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["home"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:allow-open",
    "opener:default"
  ]
}