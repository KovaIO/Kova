{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["home", "prefs", "monitor"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-hide",
    "shell:allow-open",
    "opener:default",
    "os:default"
  ]
}
